// GENERATED FILE - do not edit by hand.
//
// Class docs from packages/quark_widgets/lib/src, for the
// gallery's documentation pane. Regenerate with:
//
//     make -C packages/quark_widgets generate/docs
//
// The formatter is off so that the generator, and the test
// that checks this file is current, agree byte for byte.
// dart format off

/// Documentation for each exported widget, keyed by class name.
const Map<String, String> widgetDocs = {
  'AccessRequestsTile': 'The switch that decides whether people can ask for an account from the\nsign-in page.\n\nWhether requests are on is the caller\'s state, [enabled] in and\n[onChanged] out. While [isBusy] the switch holds still, so a second tap\ncannot race the first.\n\nKey prefixes: `access_requests_toggle` on the switch row.\n\n```dart\nAccessRequestsTile(\n  enabled: controller.accessRequestsEnabled,\n  isBusy: controller.isSavingAccessRequests,\n  onChanged: controller.setAccessRequestsEnabled,\n);\n```',
  'AddGrantForm': 'The part of a [ShareSheet] that shares the item with someone else: a\n[PrincipalPicker], a choice of level, and a share button.\n\nThe picked account or group and the level are [State] because they are\nthe form\'s own transient input; the outcome leaves through [onAdd]. The\nowner level is only on offer with [canGrantOwner].\n\nA part of [ShareSheet], tested through it.\n\nKey prefixes: `share_add_level_<level>`, `share_add_submit`, and the\n[PrincipalPicker] keys.',
  'AddToAlbumSheet': 'A draggable bottom sheet that adds one photo to albums, or takes it out.\n\nLists the whole album tree flat, each sub-album indented under its parent,\nwith a check on every album the photo is already in. Membership is the\ncaller\'s: [memberAlbumIds] in, [onToggle] out, and the caller decides\nwhether a tap adds or removes. Show it with\n`showModalBottomSheet(isScrollControlled: true, ...)`.\n\nKey prefix: `add_to_album_<id>` on each album row.\n\n```dart\nshowModalBottomSheet<void>(\n  context: context,\n  isScrollControlled: true,\n  builder: (context) => AddToAlbumSheet(\n    albums: albums,\n    memberAlbumIds: memberIds,\n    onToggle: toggleMembership,\n  ),\n);\n```',
  'AlbumPickerSheet': 'A draggable bottom sheet for choosing the album to add photos to.\n\nLists the whole album tree flat, each sub-album indented under its parent.\nIt never loads: the albums, [isLoading] and [error] come in, and the\ncaller loads again when [onRetry] fires. Show it with\n`showModalBottomSheet(isScrollControlled: true, ...)`.\n\nKey prefixes: `album_picker_<id>` on each album row and\n`album_picker_retry` on the retry button, rendered only with an [error].\n\n```dart\nshowModalBottomSheet<AlbumItem>(\n  context: context,\n  isScrollControlled: true,\n  builder: (context) => AlbumPickerSheet(\n    selectedCount: 3,\n    albums: albums,\n    onPicked: (album) => Navigator.of(context).pop(album),\n    onRetry: reload,\n  ),\n);\n```',
  'AlbumSidebar': 'The album section of a sidebar: an "Albums" header with a create button,\nand the album tree underneath.\n\nHolds nothing. The albums, the selection, and which branches are expanded\nall come in, and every action goes out, so the caller can reload the list,\nexpand a branch, or open a context menu of its own. System albums get\ntheir own glyph (a star for favorites) and no long press, since the server\nmaintains them rather than the user.\n\nBy default the list fills its parent and scrolls on its own. Set\n[shrinkWrap] wherever the parent hands it unbounded height, such as a\nsliver, where filling is a hard layout error (#1599).\n\nGive it [onAllPhotosSelected] and an "All photos" row leads the list, above\nevery album, highlighted whenever [selectedAlbumId] is null. It stays\nvisible while the albums load, fail, or turn out to be empty, since the\nlibrary is still there to go back to.\n\nKey prefixes: `album_create` on the create button,\n`album_sidebar_all_photos` on the "All photos" row, and every album row\'s\nown `album_tile_<id>` and `album_expand_<id>` from [AlbumTreeTile].\n\n```dart\nAlbumSidebar(\n  albums: controller.albums,\n  isLoading: controller.albumsLoading,\n  expandedIds: controller.expandedAlbumIds,\n  selectedAlbumId: controller.selectedAlbumId,\n  onAllPhotosSelected: controller.showAllPhotos,\n  onAlbumSelected: openAlbum,\n  onToggleExpanded: controller.toggleAlbumExpanded,\n  onCreateAlbum: promptForNewAlbum,\n  onAlbumLongPress: showAlbumMenu,\n);\n```',
  'AlbumSidebarAllPhotosTile': 'The "All photos" row that leads an `AlbumSidebar`\'s album list.\n\nDrawn like a root `AlbumTreeTile`, with a blank where the chevron would be\nso the glyphs line up, but it is not an album: no count, no long press, no\nchildren.\n\nKey prefix: `album_sidebar_all_photos` on the row.\n\n```dart\nAlbumSidebarAllPhotosTile(\n  isSelected: controller.selectedAlbumId == null,\n  onTap: controller.showAllPhotos,\n);\n```',
  'AlbumTreeTile': 'One album row, indented by depth, with its expanded sub-albums underneath.\n\nExpansion is not the tile\'s to keep: [expandedIds] comes in and\n[onToggleExpanded] goes out, so a page can expand a branch programmatically\nand the state survives the list being rebuilt. The tile recurses into\n[AlbumItem.children], passing every input down, so a whole tree is one\nwidget per root.\n\nKey prefixes: `album_tile_<id>` on the row and `album_expand_<id>` on the\ndisclosure chevron, which is only rendered when the album has children.\n\n```dart\nAlbumTreeTile(\n  album: album,\n  selectedAlbumId: controller.selectedAlbumId,\n  expandedIds: controller.expandedAlbumIds,\n  onSelected: controller.selectAlbum,\n  onToggleExpanded: controller.toggleAlbumExpanded,\n);\n```',
  'CheckerboardPainter': 'Paints the alternating squares behind `QuarkCheckerboard`\'s child.\n\nSplit out because a `CustomPainter` is not a widget and has no place in the\nwidget\'s own file. Not exported from the barrel — nothing outside\n`QuarkCheckerboard` needs it yet.',
  'ConfirmDeleteDialog': 'Asks before something is deleted: a title, a sentence on what happens,\nand a confirm button drawn in the error color.\n\nGeneric on purpose. The caller writes the copy for what is being deleted,\nand names the keys with [keyPrefix], so the same dialog confirms deleting\nan account, a group, or anything else.\n\nIt does not close itself: [onConfirm] and [onCancel] fire, and the caller\nthat pushed the dialog pops it.\n\nKey prefixes: `<keyPrefix>_confirm` and `<keyPrefix>_cancel`, for example\n`delete_user_confirm`.\n\n```dart\nshowDialog<bool>(\n  context: context,\n  builder: (ctx) => ConfirmDeleteDialog(\n    title: \'Delete bob?\',\n    body: \'Their files stay on this Quark and become yours.\',\n    keyPrefix: \'delete_user\',\n    onConfirm: () => Navigator.of(ctx).pop(true),\n    onCancel: () => Navigator.of(ctx).pop(false),\n  ),\n);\n```',
  'CopyButton': 'A copy-to-clipboard button that leaves the clipboard itself to the caller.\n\nThe package has no platform channels, so [onCopy] does the writing and\nwhatever confirmation the app shows afterwards. Pass [unavailableReason]\nwhen the clipboard cannot be used — an insecure web origin, say — and the\nbutton renders disabled with that sentence as its tooltip.\n\nKey prefixes: `copy_button` on the control itself.\n\n```dart\nCopyButton(\n  text: token,\n  onCopy: (value) => clipboard.write(value),\n  unavailableReason: isClipboardAvailable ? null : \'Use HTTPS to enable\',\n);\n```',
  'CreateUserDialog': 'The form an admin fills in to add an account: a username, an initial\npassword typed twice, and whether the account gets a private folder.\n\nIt does not close itself. [onSubmit] receives the input once every field\nvalidates, and [onCancel] fires on the cancel button; the caller that\npushed the dialog pops it, usually once the Quark has accepted the account.\nWhile that request is out the caller passes [isSubmitting], and a refusal\ncomes back in as [error], so the dialog stays open with what was typed.\n\nThe username follows the Quark\'s rule: up to 32 lowercase letters,\nnumbers, dots, dashes or underscores, starting with a letter or number. It\nis checked as typed and never rewritten, so what is submitted is exactly\nwhat the admin saw.\n\nThe text fields and the checkbox are [State] because they are the form\'s\nown transient input, thrown away when the dialog closes. The outcome\nleaves through [onSubmit].\n\nKey prefixes: `create_user_username`, `create_user_password`,\n`create_user_confirm`, `create_user_private_folder`, `create_user_cancel`,\nand `create_user_submit`.\n\n```dart\nshowDialog<void>(\n  context: context,\n  builder: (ctx) => CreateUserDialog(\n    isSubmitting: controller.isCreating,\n    error: createError,\n    onSubmit: create,\n    onCancel: () => Navigator.of(ctx).pop(),\n  ),\n);\n```',
  'EmptyStateWidget': 'The centered "there is nothing here" state: an icon, a headline, optional\nsupporting text, and an optional action.\n\nUse it wherever a list, grid, or panel loaded successfully and came back\nempty. It is not a failure state — a page that could not load composes its\nown sentence and renders a disconnected or error widget instead.\n\nEmits no `ValueKey`s of its own; the widget passed as [action] carries its\nown key.\n\n```dart\nEmptyStateWidget(\n  icon: QuarkIcons.folder_outlined,\n  headline: \'This folder is empty\',\n  subtext: \'Upload a file to get started.\',\n  action: FilledButton(onPressed: onUpload, child: const Text(\'Upload\')),\n);\n```',
  'FileBreadcrumbBar': 'The path trail above a file listing: an up button, a home glyph, and one\ntappable segment per ancestor directory.\n\nThe last segment is the current directory and is not tappable, since\ntapping it would go nowhere. The trail scrolls horizontally rather than\nwrapping, so a deep path never grows the bar\'s height on a narrow screen.\nIt renders nothing in search mode, where there is no path to show.\n\nKey prefixes: `breadcrumb_up`, `breadcrumb_home`, and\n`breadcrumb_segment_<index>` counting from zero at the shallowest.\n\n```dart\nFileBreadcrumbBar(\n  currentPath: \'/photos/2024\',\n  isSearchMode: false,\n  onGoHome: () => controller.open(\'/\'),\n  onGoUp: controller.goUp,\n  onPathSelected: controller.open,\n);\n```',
  'FileBrowserHeader': 'The strip between a file browser\'s chrome and its listing.\n\nToday it carries one thing: the search result summary, with a close button\nthat leaves search mode. Outside search mode it renders nothing, because\nthe sort headers live inside the listing itself.\n\nThe result count is an input rather than a future the widget awaits — the\npage owns the load, and a null count is the "still counting" state.\n\nKey prefixes: `file_header_close_search` on the close button.\n\n```dart\nFileBrowserHeader(\n  isSearchMode: true,\n  searchQuery: \'invoice\',\n  resultCount: results?.length,\n  onClose: controller.exitSearch,\n);\n```',
  'FileSelectionBar': 'The top bar shown in place of the usual file chrome while multi-select is\nactive: cancel, a count, select-all, and delete, with restore in front of\ndelete when [onRestore] is given (the trash offers both).\n\nThis is custom chrome rather than a real [AppBar], so it consults the\ndisplay insets itself: the [SafeArea] inside is what keeps the controls\nclear of the status bar, notch, or Dynamic Island (#1597). The surface color\nsits on the outer container so the inset region is painted rather than\nleft showing whatever is behind the bar.\n\nKey prefixes: `file_selection_cancel`, `file_selection_toggle_all`,\n`file_selection_restore`, and `file_selection_delete`.\n\n```dart\nFileSelectionBar(\n  selectedCount: controller.selected.length,\n  totalCount: controller.files.length,\n  onSelectAll: controller.selectAll,\n  onDeselectAll: controller.deselectAll,\n  onCancel: controller.exitSelection,\n  onDelete: controller.deleteSelected,\n);\n```',
  'GrantRow': 'One account or group with access in a [ShareSheet]: its name and level,\nand for access set on the item, a level menu and a remove button.\n\nAn inherited grant is read-only and names the folder it is set on.\n\nA part of [ShareSheet], tested through it.\n\nKey prefixes: `share_grant_<kind>_<id>`, `share_level_<kind>_<id>`,\n`share_level_<kind>_<id>_<level>`, `share_revoke_<kind>_<id>`, and\n`share_inherited_<kind>_<id>` on an inherited row.',
  'GroupList': 'The groups on a Quark, one row each with how many accounts are in it, the\nactions an admin can take on a group in a menu at the end of its row, and\na button for a new group.\n\nLoading, the error and the empty list are the caller\'s to decide, and each\nrenders in place of the rows; the new-group button stays. The list lays\nout as a column rather than scrolling itself, so it sits in a page\'s own\nscroll view.\n\nA built-in group, such as `everyone`, reads "Every account" and has no\nmenu: its members are every account, and it cannot be renamed or\ndeleted. An action whose callback is null is left out of every menu, and\na null [onCreate] leaves the button out.\n\nKey prefixes: `group_create` on the new-group button, `group_row_<id>` on\neach row, `group_menu_<id>` on its menu button, and\n`group_action_<action>_<id>` on the menu entries, where the action is\n`members`, `rename` or `delete`.\n\n```dart\nGroupList(\n  groups: controller.groups,\n  isLoading: controller.isLoading,\n  error: loadError,\n  busyIds: controller.busyGroupIds,\n  onCreate: openCreateDialog,\n  onMembers: openMembers,\n  onRename: openRenameDialog,\n  onDelete: confirmDelete,\n);\n```',
  'GroupMembersSheet': 'The members of one group, a button to remove each, and a picker to add an\naccount.\n\nMembership is the caller\'s: [group] in with its members, [onAdd] and\n[onRemove] out, and the caller refreshes [group] once the change lands.\nThe picker offers [candidates] less the accounts already in the group.\nA member with a change in flight, in [busyIds], shows progress in place of\nits remove button, and a refusal comes back in as [error].\n\nMeant for a group whose members can change, never a built-in one. Show it\nwith `showModalBottomSheet(isScrollControlled: true, ...)`: it scrolls\nitself and moves clear of the keyboard.\n\nKey prefixes: `group_member_<userId>` on each member row,\n`group_member_remove_<userId>` on its remove button, and the\n[PrincipalPicker] keys, `principal_search` and\n`principal_option_user_<userId>`.\n\n```dart\nshowModalBottomSheet<void>(\n  context: context,\n  isScrollControlled: true,\n  builder: (context) => GroupMembersSheet(\n    group: group,\n    candidates: activeAccounts,\n    busyIds: controller.busyMemberIds,\n    error: memberError,\n    onAdd: (userId) => controller.addMember(group.id, userId),\n    onRemove: (userId) => controller.removeMember(group.id, userId),\n  ),\n);\n```',
  'GroupRow': 'One group in a [GroupList]: its name, who is in it, and a menu of the\nactions on offer.\n\nA part of [GroupList], tested through it.\n\nKey prefixes: `group_row_<id>`, `group_menu_<id>`, and\n`group_action_<action>_<id>` on each menu entry.',
  'JobList': 'Long-running jobs, newest first, each with its status, progress, and the\nCancel or Retry action its [JobItem] offers.\n\nLoading and error are inputs. A spinner or the [error] text replaces the\nlist only while there are no [items]; with items on screen the error sits\nabove them, so a failed refresh never hides what was already known.\n\nKey prefixes: `job_row_<id>` on each row, `job_cancel_<id>` and\n`job_retry_<id>` on its actions.\n\n```dart\nJobList(\n  items: controller.items,\n  isLoading: controller.isLoading,\n  error: controller.error,\n  onCancel: cancel,\n  onRetry: retry,\n);\n```',
  'JobRow': 'One job in a `JobList`: its name, a status line, a progress bar while it\nruns, and a Cancel or Retry action when [JobItem] offers one.\n\nKey prefixes: `job_row_<id>` on the row, `job_cancel_<id>` and\n`job_retry_<id>` on the actions.',
  'JobsBadge': 'A top-bar button counting the jobs running now. Renders nothing while\n[runningCount] is zero, so it only takes space when there is news.\n\nKey prefixes: `jobs_badge` on the button.\n\n```dart\nJobsBadge(\n  runningCount: controller.runningCount,\n  onTap: () => context.go(AppRoutes.jobs),\n);\n```',
  'LiveBadge': 'The "LIVE" chip overlaid on a live photo.\n\nIt has two forms, chosen by [ready]. Left null it is the compact chip a\nthumbnail corner wants: text only, small enough to sit over a grid tile.\nGiven a value it is the larger viewer badge, with a status dot that lights\nup once the live video can play, because a full-screen viewer has room to\nsay whether the video is still loading.\n\nIts colors are deliberately fixed rather than themed: it is drawn on top of\na photograph, not on a surface, so a scrim and white text are what keep it\nreadable over whatever the image happens to be. Theme tokens would follow\nthe app into light mode and disappear against a bright picture.\n\nEmits no `ValueKey`s; the thumbnail or viewer it sits on carries the tap\ntarget.\n\n```dart\nStack(\n  children: [\n    thumbnail,\n    if (photo.hasLiveVideo)\n      const Positioned(top: 4, left: 4, child: LiveBadge()),\n    if (isViewer)\n      Positioned(top: 12, left: 12, child: LiveBadge(ready: videoReady)),\n  ],\n);\n```',
  'NewFileDialog': 'The body of the new-file dialog: a type picker and a validated name field.\n\nIt does not close itself. [onCreate] receives the finished filename with\nthe chosen extension already appended, and [onCancel] fires on the cancel\nbutton; the caller that pushed the dialog is the one that pops it.\n\nThe selected type and the typed name are [State] because they are a form\'s\nown transient input, thrown away the moment the dialog closes. Nothing\noutside the dialog can observe them, and the one outcome that matters\nleaves through [onCreate].\n\nKey prefixes: `new_file_type_<extension without the dot, or `generic`>`,\n`new_file_name`, `new_file_cancel`, and `new_file_create`.\n\n```dart\nshowDialog<String>(\n  context: context,\n  builder: (ctx) => NewFileDialog(\n    onCreate: (name) => Navigator.of(ctx).pop(name),\n    onCancel: () => Navigator.of(ctx).pop(),\n  ),\n);\n```',
  'NewFileTypeCard': 'One selectable file type in [NewFileDialog]\'s type picker.\n\nKey prefix: `new_file_type_<extension without the dot, or `generic`>`.',
  'PasswordStrengthBar': 'An animated bar showing how strong [password] is, with the level named\nunderneath.\n\nPlace it beneath a password field. It is advisory only and never blocks a\nform: validation stays with the field.\n\nEmits no `ValueKey`s; it is not interactive.\n\n```dart\nPasswordStrengthBar(password: passwordController.text);\n```',
  'PendingRequestList': 'Account requests waiting for an admin, one row each with approve and deny.\n\nLoading, the error and the empty list are the caller\'s to decide, and each\nrenders on its own. The rows lay out as a column, so the list sits in a\npage\'s own scroll view with the sections around it.\n\nKey prefixes: `request_row_<username>` on each row, and\n`request_approve_<username>` and `request_deny_<username>` on its buttons.\n\n```dart\nPendingRequestList(\n  requests: controller.pending,\n  busyUsernames: controller.busyUsernames,\n  onApprove: approve,\n  onDeny: deny,\n);\n```',
  'PendingRequestRow': 'One request in a [PendingRequestList]: who asked, and approve and deny.\n\nA part of [PendingRequestList], tested through it.\n\nKey prefixes: `request_row_<username>`, `request_approve_<username>`, and\n`request_deny_<username>`.',
  'PhotoCategoryList': 'The "Showing" picker in a photo sidebar: the category being shown and its\ncount, which expands into every category to choose from.\n\nWhether it is expanded is the caller\'s state, [expanded] in and\n[onToggleExpanded] out, so choosing a category can collapse it again.\n\nKey prefixes: `photo_category_toggle` on the "Showing" row, and\n`photo_category_<id>` on each category row, rendered only while [expanded].\n\n```dart\nPhotoCategoryList(\n  categories: controller.categories,\n  selectedId: controller.selectedCategoryId,\n  expanded: controller.categoriesExpanded,\n  onToggleExpanded: controller.toggleCategoriesExpanded,\n  onSelected: controller.selectCategory,\n);\n```',
  'PhotoGrid': 'A grid of [PhotoGridTile]s, as a sliver, with its loading, error, empty\nand loading-more states.\n\nA sliver rather than a box so it can share one scroll view with a\ncollapsed sidebar above it, which is how `QuarkSplitView` lays out a narrow\nwindow. Put it in that widget\'s `slivers`, or in any `CustomScrollView`.\n\nIt never decides when to load. [isLoading], [error], [hasMore] and\n[isLoadingMore] come in, and the caller fetches the next page when its\nscroll view nears the end. The states, in order of precedence:\n\n- [isLoading] with no photos yet: a centered spinner\n- [error]: the message, centered\n- no photos and nothing loading: [emptyState], which the caller words\n- otherwise the grid, with one more cell holding a small spinner while\n  [hasMore] says there is another page\n\nGestures are reported by index into [photos]. [onDoubleTap] is offered on\nremote photos outside selection mode only, since those are the only ones\nthat can be favorited; everywhere else a single tap fires immediately.\n\nKey prefixes: `photo_grid` on the grid, `photo_grid_loading_more` on the\ntrailing spinner cell, and every tile\'s own `photo_tile_<id>` and\n`photo_tile_check_<id>`.\n\n```dart\nCustomScrollView(\n  slivers: [\n    PhotoGrid(\n      photos: controller.photos,\n      crossAxisCount: 4,\n      selectedIds: controller.selectedIds,\n      hasMore: controller.hasMore,\n      emptyState: const Text(\'No photos yet\'),\n      thumbnailBuilder: (context, photo) => Image.network(urlFor(photo)),\n      onTap: open,\n      onLongPress: (index) => controller.select(index),\n    ),\n  ],\n);\n```',
  'PhotoGridTile': 'One square of a photo grid: the thumbnail, a live badge, a favorite star,\nand the selection chrome while photos are being selected.\n\nThe tile only reports gestures. What a tap means — opening the photo or\ntoggling it in the selection — is the caller\'s decision, so [onTap],\n[onLongPress] and [onDoubleTap] fire the same way in and out of\n[selectionMode]. [selectionMode] and [isSelected] only change what is drawn:\na dim over every unselected photo, a primary border around every selected\none, and a checkbox in the corner.\n\nThe thumbnail comes from [thumbnailBuilder], because drawing one needs the\nnetwork or a device API the package does not depend on.\n\nThe overlay colors are fixed rather than themed on purpose: like\n[LiveBadge], they are drawn on top of a photograph, not on a surface.\n\nKey prefixes: `photo_tile_<id>` on the tile and `photo_tile_check_<id>` on\nthe selection checkbox, which is only rendered in [selectionMode].\n\n```dart\nPhotoGridTile(\n  item: photo,\n  isSelected: controller.selectedIds.contains(photo.id),\n  selectionMode: controller.selectionMode,\n  thumbnailBuilder: (context, photo) => Image.network(urlFor(photo)),\n  onTap: () => open(photo),\n  onLongPress: () => controller.toggleSelection(photo.id),\n);\n```',
  'PhotoLibrarySidebar': 'The sidebar of a photo library: a tile-size slider, an optional category\npicker, and an album list underneath.\n\nBuilt to sit in a [QuarkSplitView], and it asks that widget which shape to\ntake rather than being told, so nothing upstream has to know the\nbreakpoint. Wide, it fills the pane and gives [albums] the remaining\nheight. Collapsed, it sits in a sliver with unbounded height, so it\nshrink-wraps and never wraps [albums] in an [Expanded] (#1599). [albums]\nhas to shrink-wrap there too.\n\nThe column count is the caller\'s: [columns] in, [onColumnsChanged] out,\nalready clamped to [minColumns] and [maxColumns].\n\nKey prefixes: `photo_columns_slider`, `photo_columns_less` (larger\nphotos) and `photo_columns_more` (smaller photos).\n\n```dart\nPhotoLibrarySidebar(\n  columns: controller.columns,\n  minColumns: 1,\n  maxColumns: 8,\n  onColumnsChanged: controller.setColumns,\n  categories: PhotoCategoryList(...),\n  albums: AlbumSidebar(...),\n);\n```',
  'PhotoSelectionBar': 'The bar that sits at the bottom of the photo grid while photos are being\nselected: cancel, a count, and the add-to-album action.\n\nAdding is disabled at a count of zero, so the button never opens a picker\nthat would do nothing.\n\nKey prefixes: `photo_selection_cancel` and `photo_selection_add_to_album`.\n\n```dart\nPhotoSelectionBar(\n  selectedCount: controller.selectedKeys.length,\n  onAddToAlbum: () => showAlbumPicker(context),\n  onCancel: controller.exitSelectionMode,\n);\n```',
  'PrincipalPicker': 'A searchable list of accounts and groups to pick one from, for adding a\nmember to a group or choosing who to share with.\n\nThe options are listed in the order given, and typing in the search field\nnarrows them to names containing the text, ignoring case. Which option is\npicked is the caller\'s: [selected] in, [onSelected] out. The search text\nis [State] because it is the field\'s own transient input.\n\nThe options lay out as a column rather than scrolling themselves, so the\npicker sits in a sheet\'s or a page\'s own scroll view.\n\nKey prefixes: `principal_search` on the search field, and\n`principal_option_<kind>_<id>` on each option, such as\n`principal_option_user_3` or `principal_option_group_1`.\n\n```dart\nPrincipalPicker(\n  options: controller.principals,\n  selected: picked,\n  onSelected: (principal) => setState(() => picked = principal),\n);\n```',
  'QuarkAppBar': 'The app bar every main page wears: a [QuarkBrandButton] on the left that\nopens the drawer, no title, and the page\'s own actions on the right.\n\nThe theme toggle is not built in. It reads the app\'s settings, so the page\nappends its own wired copy to [actions] and the package stays free of app\nstate.\n\nApp-wide controls, such as a running-jobs badge, come from a\n[QuarkAppBarTrailing] scope and follow [actions].\n\nKey prefixes: `brand_button`, from the [QuarkBrandButton] it renders.\n\n```dart\nScaffold(\n  appBar: QuarkAppBar(\n    label: \'Photos\',\n    icon: QuarkIcons.photo_library_outlined,\n    actions: [RefreshIconButton(...), const AppThemeToggle()],\n  ),\n);\n```',
  'QuarkAppBarTrailing': 'App-wide controls every main page\'s bar shows after its own actions, such\nas a running-jobs badge.\n\nThe package cannot read app state, so the app provides this once, above\nits pages, and `QuarkAppBar` reads it. Without a scope the bars render\nonly their own actions, which is what the gallery and tests see.\n\nEmits no `ValueKey`s of its own; each widget in [actions] carries its own.\n\n```dart\nMaterialApp.router(\n  builder: (context, child) => QuarkAppBarTrailing(\n    actions: [JobsBadge(runningCount: count, onTap: openJobs)],\n    child: child!,\n  ),\n);\n```',
  'QuarkBrandButton': 'The branded badge-and-label control that leads every main page\'s top bar.\n\nIt is a button, not a title: tapping it is how the app opens the navigation\ndrawer. When used as [AppBar.leading], set [AppBar.leadingWidth] to at least\n[QuarkBrandButton.preferredWidth] or the row overflows.\n\nKey prefixes: `brand_button` on the control itself.\n\n```dart\nQuarkBrandButton(\n  label: \'Files\',\n  onTap: () => Scaffold.of(context).openDrawer(),\n);\n```',
  'QuarkCheckerboard': 'The alternating squares image editors paint behind artwork to say "these\npixels are transparent".\n\nA transparent PNG or SVG drawn straight onto a page shows the page\'s own\nbackground through its holes, so the artwork reads as if it were on a solid\ncanvas — and a white logo on the dark theme looks identical to an opaque\none. The checkerboard is the conventional answer: it is busy enough to be\nobviously not part of the image, and neutral enough that light and dark\nartwork both stay readable on it.\n\nThe two colors come from [QuarkTokens.card] and [QuarkTokens.border], so the\nboard follows the theme: near-white against pale gray in the light theme,\nnavy against a lighter navy in the dark one. Both pairs are one step apart\non the neutral ramp — enough to read as a grid, not enough to compete with\nwhat sits on top.\n\n[child] is painted over the board and decides the size, so wrap it in\nsomething that fills the space (a [SizedBox.expand], a [Center], a\n[Positioned.fill]) when the board should cover more than the child does.\n\nKey prefixes: none. Nothing here is tappable, so there is nothing for a\n`.probe` script to address — find it by type in a test.\n\n```dart\nQuarkCheckerboard(\n  child: InteractiveViewer(\n    child: Center(child: SvgPicture.memory(bytes, fit: BoxFit.contain)),\n  ),\n);\n```',
  'QuarkDisconnectedBanner': 'Compact disconnected state, for a page that stays usable while\ndisconnected — Settings and login, where the fix lives on the page itself.\n\nKey prefixes: `disconnected_banner_retry` on the retry button, rendered\nonly when [onRetry] is supplied.\n\n```dart\nQuarkDisconnectedBanner(onRetry: controller.refresh);\n```',
  'QuarkDisconnectedView': 'Full-page disconnected state, for a page with nothing left to show.\n\nKey prefixes: `disconnected_retry` and `disconnected_manage_hosts` on the\ntwo buttons, each rendered only when its callback is supplied.\n\n```dart\nQuarkDisconnectedView(\n  hostAddress: settings.activeHost,\n  onRetry: controller.refresh,\n  onManageHosts: () => context.go(AppRoutes.settings),\n);\n```',
  'QuarkDrawer': 'The app\'s navigation drawer: one row per [QuarkDrawerSection] the caller\noffers, with the current one marked.\n\nThe drawer navigates nothing itself. Each row calls back and the page\nroutes, so the package stays free of the router.\n\nA row is drawn only when its callback is set. That is how a caller hides a\ndestination the signed-in user cannot use, such as Users or Vault for\nsomeone who is not an admin, rather than showing a row that goes nowhere.\n\nKey prefixes: `drawer_<section>` on each row, for example `drawer_photos`\nand `drawer_users`.\n\n```dart\nQuarkDrawer(\n  activeSection: QuarkDrawerSection.photos,\n  onTapFiles: () => context.go(AppRoutes.files),\n  onTapUsers: isAdmin ? () => context.go(AppRoutes.users) : null,\n);\n```',
  'QuarkFileIcon': 'The glyph for a file or folder, chosen from its name.\n\nThe package takes the name and the directory flag rather than an app model,\nso callers map their own node type at the call site:\n`QuarkFileIcon(name: node.name, isDir: node.isDir)`.\n\nEmits no `ValueKey`s; it is decoration inside a row that carries its own.\n\n```dart\nQuarkFileIcon(name: \'budget.qsheet\', isDir: false, size: 24);\n```',
  'QuarkNameDialog': 'Asks for one name, to create something or rename it: a title, a text\nfield, and a submit button.\n\nGeneric on purpose. The caller writes the title, the field label and the\nsubmit label, so the same dialog names a group or anything else that has\na name.\n\nIt does not close itself. [onSubmit] receives the trimmed name, and\n[onCancel] fires on the cancel button; the caller that pushed the dialog\npops it, usually once the Quark has accepted the name. While that request\nis out the caller passes [isSubmitting], and a refusal comes back in as\n[error], so the dialog stays open with what was typed.\n\nThe typed text is [State] because it is the form\'s own transient input,\nthrown away when the dialog closes. The outcome leaves through [onSubmit].\n\nKey prefixes: `name_dialog_field`, `name_dialog_cancel`, and\n`name_dialog_submit`.\n\n```dart\nshowDialog<void>(\n  context: context,\n  builder: (ctx) => QuarkNameDialog(\n    title: \'New group\',\n    label: \'Group name\',\n    submitLabel: \'Create\',\n    maxLength: 64,\n    isSubmitting: controller.isSaving,\n    error: saveError,\n    onSubmit: create,\n    onCancel: () => Navigator.of(ctx).pop(),\n  ),\n);\n```',
  'QuarkPageScaffold': 'The shape every top-level Quark page wears: the shared app bar, the\nnavigation drawer, a body, and an optional bar pinned to the bottom.\n\nPages used to build this `Scaffold` themselves, which is how eight of them\nended up with eight slightly different ideas of what a page is. The bar,\nthe drawer slot, and the bottom inset are decided here once.\n\nThe default bar is a [QuarkAppBar] built from [title], [icon], and\n[actions]. A page that swaps its bar out for a mode of its own, a selection\nbar for instance, passes [appBar] instead, and [title], [icon], and\n[actions] are then unused.\n\n[bottomBar] is laid out inside a [SafeArea], so a bar handed to it clears\nthe home indicator without the caller thinking about insets. The body is\nleft alone: its content scrolls under the system bars, which is what a\nphoto grid wants.\n\nKey prefixes: `brand_button`, from the [QuarkAppBar] it builds. The\nscaffold has nothing tappable of its own.\n\n```dart\nQuarkPageScaffold(\n  title: \'Photos\',\n  icon: QuarkIcons.photo_library_outlined,\n  actions: [RefreshIconButton(isRefreshing: false, onPressed: reload)],\n  drawer: QuarkDrawer(activeSection: QuarkDrawerSection.photos),\n  bottomBar: PhotoSelectionBar(selectedCount: 3, ...),\n  body: QuarkSplitView(sidebar: sidebar, slivers: slivers),\n);\n```',
  'QuarkSection': 'A titled block of a page: a heading, whatever actions belong to it, and the\ncontent under both.\n\nEvery page grew its own version of this out of a bold `Text`, a `SizedBox`,\nand a `Row` — settings alone had eight, in two different sizes. The heading\nstyle, the gap under it, and what happens to the actions on a narrow phone\nare decided here instead.\n\nThe actions go through a [QuarkToolbar], so a heading with three buttons\nwraps them onto a second line rather than overflowing at 360 pixels.\n\nKey prefixes: `section_<title>`, with the title lowercased and every run of\nnon-alphanumeric characters turned into a single underscore, so\n`QuarkSection(title: \'Help & Support\')` is `#section_help_support`. The\nactions carry their own keys.\n\n```dart\nQuarkSection(\n  title: \'Backend hosts\',\n  actions: [IconButton(icon: const Icon(Icons.add), onPressed: addHost)],\n  child: HostList(hosts: hosts),\n);\n```',
  'QuarkSplitView': 'A sidebar beside a scrolling content pane on a wide window, and the same\nsidebar stacked above that content on a narrow one.\n\nThis is the one place the sidebar breakpoint is written down. Above\n[collapseBreakpoint] the sidebar is a fixed [sidebarWidth] pane on the\nleading edge with a divider between it and the content. Below it the pane\nwould leave the content a sliver of screen, so the sidebar collapses into\nthe top of the content\'s own scroll view: scroll up and it is there, scroll\ndown and the content has the whole viewport.\n\nThe content is a sliver list rather than a widget so both layouts share one\nscroll view, which is what lets the collapsed sidebar scroll away with the\ncontent instead of stealing height from it. Handing a sliver an unbounded\nheight is also what made the photos view render nothing under 900px, so the\ncollapsed sidebar is shrink-wrapped here and never given an [Expanded]\n(#1599).\n\nWhether the collapsed sidebar is showing is the caller\'s state:\n[isSidebarOpen] in, [onToggleSidebar] out. The widget holds no state of its\nown, and a caller that would rather present the sidebar as a drawer or a\nsheet passes `isSidebarOpen: false` and shows its own.\n\nKey prefixes:\n- `split_view_sidebar` for the sidebar pane, in either layout\n- `split_view_toggle` for the show/hide button, rendered only when the\n  layout is collapsed and [onToggleSidebar] is supplied\n\n```dart\nQuarkSplitView(\n  sidebar: AlbumSidebar(selectedAlbumId: albumId),\n  controller: scrollController,\n  slivers: [SliverGrid(...)],\n);\n```',
  'QuarkStorageBar': 'A horizontal capacity meter whose fill color escalates as it fills:\nprimary while there is room, warning past three quarters, error past nine\ntenths.\n\nThe fill animates between values, so handing it a new [usedFraction] on a\nrefresh reads as movement rather than a jump.\n\nEmits no `ValueKey`s; it is not interactive.\n\n```dart\nQuarkStorageBar(usedFraction: used / total);\n```',
  'QuarkTabView': 'A row of tabs over the content of the selected one, for a page split into\na few views of the same subject, like the accounts and groups on the Users\npage.\n\nThe first tab starts selected. Which tab is showing is the view\'s own\nvisual state, kept by a [DefaultTabController]: nothing outside the view\ndepends on it.\n\nIt fills the height it is given, so it goes where the height is bounded,\nsuch as a page scaffold\'s body, and each tab\'s content scrolls itself.\n\nKey prefixes: `tab_<label>` on each tab, with the label turned into a slug\nby the same rule as [QuarkSection.slug], so a tab labeled `Groups` is\n`#tab_groups`.\n\n```dart\nQuarkTabView(\n  tabs: [\n    QuarkTab(label: \'Accounts\', child: ListView(children: accounts)),\n    QuarkTab(label: \'Groups\', child: ListView(children: groups)),\n  ],\n);\n```',
  'QuarkTokens': 'The design tokens every Quark widget draws from: colors, corner radii, and\na spacing scale.\n\nTokens are values, not constants, so they can be edited at runtime — the\nwidget gallery\'s theme panel rebuilds the whole app from an edited\n[QuarkTokens]. A widget that hardcodes a color instead of reading a token\nstops following the panel, which is how you spot it.\n\nReach them through the theme:\n\n```dart\nfinal tokens = QuarkTokens.of(context);\nContainer(\n  color: tokens.card,\n  padding: EdgeInsets.all(tokens.spacingMd),\n);\n```\n\n[QuarkTokens.dark] and [QuarkTokens.light] are the two sets the app ships.',
  'QuarkToolbar': 'A row of actions that never overflows: it wraps onto another line, or\nscrolls sideways, depending on [overflow].\n\nA plain `Row` of buttons is fine until someone opens the app on a 360\npixel phone, at which point it throws a layout error and paints the yellow\nstripes over the controls. Every bar in the app solved that separately.\nThis solves it once, so a new bar starts out narrow-safe.\n\nThe toolbar spaces its actions with the theme\'s small spacing token.\n\nKey prefixes: none of its own. The toolbar has nothing tappable, so the\nkeys a test or a `.probe` script reaches for are the ones the actions\ncarry.\n\n```dart\nQuarkToolbar(\n  actions: [\n    IconButton(key: const ValueKey(\'files_refresh\'), ...),\n    FilledButton(onPressed: upload, child: const Text(\'Upload\')),\n  ],\n);\n```',
  'RefreshIconButton': 'A refresh [IconButton] that swaps its glyph for a spinner while a refresh\nis in flight, and refuses taps until it finishes.\n\nWhether a refresh is running is an input, not something the button tracks:\nthe page owns the load.\n\nKey prefixes: `refresh_button` on the control itself.\n\n```dart\nRefreshIconButton(\n  isRefreshing: controller.isLoading,\n  onPressed: controller.refresh,\n);\n```',
  'ScrollUpHint': 'A chevron fading down from the top edge, hinting that there is more above.\n\nFor a scroll view that starts scrolled past its first content, such as a\ncollapsed sidebar parked above a photo grid, where nothing else on screen\nsays the content is there. Never takes a pointer, so it can sit over\nwhatever it overlaps. The caller decides when to stop showing it.\n\nKey prefix: `scroll_up_hint` on the hint itself.\n\n```dart\nStack(\n  children: [\n    scrollView,\n    if (showHint)\n      const Positioned(top: 0, left: 0, right: 0, child: ScrollUpHint()),\n  ],\n);\n```',
  'ShareSheet': 'Who has access to one file or folder, controls to change it, and a form\nto share it with another account or group.\n\nEvery value is the caller\'s: [grants] and [principals] in, [onAdd],\n[onSetLevel] and [onRevoke] out, and the caller replaces [grants] once a\nchange lands. A row with a change in flight, in [busyKeys], shows progress\nin place of its controls.\n\nAccess set on the item itself is listed first, each row with a level menu\nand a remove button. Access inherited from a folder the item is in is\nlisted under **Inherited access**, read-only and naming that folder,\nbecause it can only be changed there. The same account or group can\nappear in both lists.\n\nOnly a caller who manages sharing, [canManage], gets the form and working\ncontrols. The owner level, and changing an owner row, also needs\n[canGrantOwner]. A row whose [PrincipalItem.keySuffix] is in [lockedKeys]\nstays read-only either way, such as the caller\'s own ownership.\n\nThe caller confirms whatever deserves it, such as removing an owner,\nbefore acting on a callback. Show the sheet with\n`showModalBottomSheet(isScrollControlled: true, ...)`: it scrolls itself\nand moves clear of the keyboard.\n\nKey prefixes, where `<kind>_<id>` is [PrincipalItem.keySuffix]:\n`share_grant_<kind>_<id>` on each row set on the item,\n`share_level_<kind>_<id>` on its level menu,\n`share_level_<kind>_<id>_<level>` on the menu entries,\n`share_revoke_<kind>_<id>` on its remove button,\n`share_inherited_<kind>_<id>` on each inherited row,\n`share_add_level_<level>` on the form\'s level choices, `share_add_submit`\non its share button, and the principal picker\'s `principal_search` and\n`principal_option_<kind>_<id>`.\n\n```dart\nshowModalBottomSheet<void>(\n  context: context,\n  isScrollControlled: true,\n  builder: (context) => ShareSheet(\n    itemName: \'Recipes\',\n    grants: controller.grants,\n    principals: controller.principals,\n    canManage: controller.canManage,\n    canGrantOwner: controller.canGrantOwner,\n    lockedKeys: controller.lockedKeys,\n    busyKeys: controller.busyKeys,\n    error: shareError,\n    onAdd: add,\n    onSetLevel: setLevel,\n    onRevoke: confirmRevoke,\n  ),\n);\n```',
  'ThemeToggleButton': 'An [IconButton] that switches the app between light and dark.\n\nThe current [mode] comes in and the chosen one goes out; the package never\nreads or writes the app\'s settings. From [ThemeMode.system] the button\ncommits to light, because the first tap is a user saying they want the\nother one, not the one they are already looking at.\n\nKey prefixes: `theme_toggle` on the control itself.\n\n```dart\nThemeToggleButton(\n  mode: settings.themeMode.value,\n  onChanged: settings.setThemeMode,\n);\n```',
  'TranscodeDialog': 'A dialog for converting a video: choose a quality and a target format.\n\nIt never loads: [formats], [isLoading], and [error] come in, and the caller\nloads again when [onRetry] fires. The chosen format and quality are [State]\nbecause they are the form\'s own transient input, thrown away when the\ndialog closes; the one outcome that matters leaves through [onConvert].\n\nAt [TranscodeQuality.original] the video\'s own format, [sourceFormat], is\nshown but disabled, since converting to it would only copy the file.\nChoosing it at [TranscodeQuality.small] and then switching back to original\nclears the choice. Chips carry no checkmark and every quality description\nreserves the same height, so nothing in the dialog moves under a tap.\n\nKey prefixes: `transcode_quality_<quality>` on each quality chip,\n`transcode_format_<format>` on each format chip, `transcode_convert`,\n`transcode_cancel`, and `transcode_retry`, rendered only with an [error].\n\n```dart\nshowDialog<(String, TranscodeQuality)>(\n  context: context,\n  builder: (context) => TranscodeDialog(\n    formats: formats,\n    sourceFormat: \'mov\',\n    onConvert: (format, quality) =>\n        Navigator.of(context).pop((format, quality)),\n    onCancel: () => Navigator.of(context).pop(),\n    onRetry: reload,\n  ),\n);\n```',
  'TroubleshootingList': 'The bulleted checklist, left-aligned so the lines scan as a list rather\nthan as more centered prose.',
  'UploadTargetPicker': 'The body of a bottom sheet for choosing which device an upload goes to.\n\nA radio list of [targets] with cancel and upload buttons. The choice is the\ncaller\'s: [selected] in, [onSelected] out, and [onConfirm] fires when the\nuser commits to it.\n\nKey prefixes: `upload_target_<index>` on each radio row (by position, since\na default device may have no serial), `upload_target_cancel` and\n`upload_target_confirm` on the buttons.\n\n```dart\nUploadTargetPicker(\n  targets: targets,\n  selected: chosen,\n  onSelected: (target) => setState(() => chosen = target),\n  onCancel: () => Navigator.of(context).pop(),\n  onConfirm: () => Navigator.of(context).pop(chosen),\n);\n```',
  'UserList': 'The accounts on a Quark, one row each, with the actions an admin can take\non an account in a menu at the end of its row.\n\nLoading, the error and the empty list are the caller\'s to decide, and each\nrenders on its own. The list lays out as a column rather than scrolling\nitself, so it sits in a page\'s own scroll view with the sections around it.\n\nThe signed-in account\'s row, [selfUsername], never offers actions: an admin\nchanges their own account from Settings. An action whose callback is null\nis left out of every menu, and a row whose menu would be empty has none.\n\nKey prefixes: `user_row_<username>` on each row, `user_menu_<username>` on\nits menu button, and `user_action_<action>_<username>` on the menu\nentries, where the action is `promote`, `demote`, `disable`, `enable` or\n`delete`.\n\n```dart\nUserList(\n  users: controller.accounts,\n  selfUsername: controller.selfUsername,\n  isLoading: controller.isLoading,\n  error: loadError,\n  busyUsernames: controller.busyUsernames,\n  onPromote: promote,\n  onDemote: demote,\n  onDisable: disable,\n  onEnable: enable,\n  onDelete: confirmDelete,\n);\n```',
  'UserRow': 'One account in a [UserList]: its name, what it is, and a menu of the\nactions on offer.\n\nA part of [UserList], tested through it.\n\nKey prefixes: `user_row_<username>`, `user_menu_<username>`, and\n`user_action_<action>_<username>` on each menu entry.',
};
